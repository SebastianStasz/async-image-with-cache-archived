    import XCTest
    @testable import AsyncImageWithCache

    final class AsyncImageWithCacheTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(AsyncImageWithCache().text, "Hello, World!")
        }
    }
